export default function Observation({metricName, itemId, lotId, productId, productMetricConfiguration, 
    previousValues, onChange }) {

    //Data entry modes:
    // scary - submit immediately, no delay
    // delay - submit after n seconds
    // double entry - submit immediately after matching double entry
    // item submit - submit on click for invidivual item
    // group submit - submit when button for group is clicked (fire onChange)
    // page submit - submit when button for page is clicked (fire onChange)

    return (
        <div>

        </div>
    )
}